# 14 — Экономика (The Economy): валюты, кривые, рынок спроса, анти-инфляция

> **Канон:** `docs/specs/00-canon.md` (версия `1.0`) — источник истины по неймингу, валютам (§2.1) и опорным эконом-цифрам (§2.2). Все числа ниже, не взятые дословно из канона, помечены `(гипотеза)` — это **централизующая** спека: именно здесь финализируются `(гипотеза)`-числа всех остальных доков (цены блюд, стоимости апгрейдов, кэпы, циклы).
>
> **Роль дока:** это самый строгий эконом-контракт проекта. Профильные спеки (`02-farm`, `03-animals`, `04-machines`, `05-ingredients`, `07-expeditions`, `08-mail-foraging`, `09-fair`) дают локальные прогрессии и тайминги; **абсолютные `$`-цены, кривые валют, рыночный спрос, прайсинг Dimes и анти-инфляция живут здесь**.
>
> **Внимание — номер файла.** Все сиблинг-спеки ссылаются на экономику как `docs/specs/12-economy.md`. Этот файл — `14-economy.md`. Расхождение нумерации вынесено в §8 (Открытые вопросы) как блокер консистентности; до его разрешения считать `12-economy` и `14-economy` **одним и тем же документом**.

---

## 1. TL;DR

- **Четыре валюты, четыре непересекающиеся роли (канон §2.1):** `$` **Bucks (Баксы)** — вся производственная экономика; `◉` **Dimes (Даймы)** — премиум-время/слоты/косметика (за реал); `🎟` **Tickets (Тикеты)** — ивент-котёл/лавка/страховка стрика; `🎀` **Ribbons (Ленты)** — чистый престиж (только конкурсы → только витрина). **Валюты не конвертируются напрямую в силу** (гардрейл D11): нельзя купить Bucks за Dimes, нельзя купить победу за деньги.
- **Ядро кривой ценности (канон §2.2):** блюдо дорожает **×150 за клик** T1→T5 (`$6 → $900`), но доход/час растёт лишь **×2.5** — потому что высокий тир крафтится дольше. Итог: прогресс = **меньше кликов, больше планирования**, а не бесконечный гринд.
- **Синки экспоненциальны:** апгрейды земли/станков/слотов дорожают **×2.2 за уровень**. Это главный дренаж Bucks, держащий инфляцию в узде: чем больше игрок зарабатывает, тем дороже следующий шаг.
- **Рынок спроса (Demand Board) — сердце экономики.** Каждую неделю 4 мета-категории получают множитель `D_cat ∈ [0.70, 1.30]` (±15–30%, канон §2.3), детерминированно от **сида `hash(номер_недели, town_id)`** — одинаково для всего города, зеро-сумно (в среднем 1.0). Плюс еженедельные **ностальгия-бонусы ×2** на 1–2 продукта T1–T2, чтобы огород новичка был нужен городу (E9).
- **Перенасыщение мягко дешевит:** если город завалил категорию, эффективная цена падает по формуле `S_sat` (пол 0.40, потолок 1.15) — доминировать одной метой невыгодно, но не смертельно (E7).
- **Катч-ап новичка:** **Grand Opening ×2** к Bucks-доходу на 7 дней (старт, мердж, караван) — `mech_grand_opening`.
- **Прайсинг Dimes = функция оставшегося времени:** `dimes = ceil(0.41 × t_min^0.53)` — короткий таймер почти бесплатен, длинный дорожает по вогнутой кривой; последняя минута — бесплатно.

---

## 2. Player Experience (как это ощущается)

### 2.1 Деньги как ритм, а не как счётчик
Игрок почти никогда не видит «пусто на счету». Bucks капают пассивно (прилавок продаёт сток офлайн, `09-fair`), а тратятся крупными осмысленными рывками: «коплю на второй гриль», «ещё $2,000 — и открою Icehouse». Экономика ощущается как **накопление на мечту**, а не как борьба с дефицитом. Провала-банкротства нет: цены только растут вверх, отрицательного баланса не бывает (нельзя купить в минус).

### 2.2 Рынок как еженедельная интрига
Понедельник — маленький праздник открытия конверта: DJ Ricky Ray по радио WSUN объявляет спрос недели. «На этой неделе округ сходит с ума по десертам (+25%), а гриль просел (−15%)». Игрок и стрит **планируют неделю под спрос**: кто сеет вишню под пироги, кто держит гриль на кооп-заказ. Ностальгия-бонус («домашний лимонад T1 ×2!») превращает копеечный огород в горячий товар — новичок внезапно нужен всей улице.

### 2.3 Dimes — «купить нетерпение», не «купить силу»
Даймы ощущаются как **уважительная опция для занятых**, а не как pay-to-win. Ими докидывают последний час до готовности партии перед ярмаркой, открывают шестую грядку, красят грузовик в Tiki-стиль. Кит, спускающий Dimes на бусты перед субботой, **кормит общий котёл города** и получает бейдж «Меценат ярмарки» (Fair Patron) — статус, не превосходство (гардрейл §4 канона).

### 2.4 Ленты и тикеты — разные удовольствия
`🎀` Ribbons вешаются на Ribbon Wall с выгравированным годом — это личная история побед, её нельзя купить и нельзя потратить. `🎟` Tickets — «ивент-кошелёк»: накопил на ярмарочной лавке редкий декор, застраховал стрик перед отпуском. Обе валюты дают **тепло владения**, не циферку мощи (пилларс P4).

---

## 3. Механики — исчерпывающе

### 3.1 Реестр валют: роли, ограничения, конвертация

| Валюта | Ключ | Символ | Тип | Можно купить за реал? | Конвертируется в | НЕ конвертируется в |
|---|---|---|---|---|---|---|
| Bucks (Баксы) | `bucks` | `$` | Soft | Нет (только зарабатывается) | — | Dimes, Tickets, Ribbons |
| Dimes (Даймы) | `dimes` | `◉` | Premium/hard | **Да** | время, слоты, косметику, Prize-пуллы | **напрямую в Bucks — запрещено (анти-P2W, D11)** |
| Tickets (Тикеты) | `tickets` | `🎟` | Event | Нет (косвенно — бусты кормят котёл) | ивент-товары, страховку стрика | Bucks, Dimes, Ribbons |
| Ribbons (Ленты) | `ribbons` | `🎀` | Prestige | Нет | **ничто** (только отображаются) | что угодно |

**Жёсткие правила конвертации (анти-P2W, канон D11):**
1. `Dimes → Bucks` прямого обмена **нет**. Dimes покупают только *время* (ускорения) и *ёмкость* (слоты) — Bucks игрок всё равно зарабатывает сам.
2. `Bucks → Dimes` невозможен (софт не покупает хард).
3. `🎟`/`🎀` — замкнутые контуры: тикеты тратятся только в ивент-лавке/на страховку, ленты не тратятся вовсе.
4. Единственный «мост» — **время**: Dimes сокращают таймеры, косвенно ускоряя Bucks-доход. Это продажа удобства, не силы.

### 3.2 Кривая ценности блюда по тирам (ядро, канон §2.2)

Опорные цены и циклы — из канона §2.2 (здесь финализируются):

| Тир | `p_ref` цена, `$` | ×к T1 (ценность/клик) | Цикл, мин | Валовый `$`/час на 1 слот | ×к T1 (доход/час) |
|---|---|---|---|---|---|
| T1 Garden (Огород) | 6 | ×1 | 5 | 72 | ×1.00 |
| T2 Farm (Ферма) | 22 | ×3.7 | 15 | 88 | ×1.22 |
| T3 County (Округ) | 75 | ×12.5 | 45 | 100 | ×1.39 |
| T4 States (Штаты) | 260 | ×43 | 120 | 130 | ×1.81 |
| T5 Legends (Легенды) | 900 | ×150 | 300 | 180 | **×2.50** |

**Формулы:**
```
value_per_click(tier) = p_ref(tier)                     // ×150 за клик T1→T5
gross_per_hour(tier)  = p_ref(tier) / (cycle_min/60)    // ×2.5 за час T1→T5
```
> **Дизайн-контракт (канон §2.2):** «×150 ценность за клик, ×2.5 доход/час». Высокий тир не «выгоднее в час» — он **экономит клики и требует планирования** (одна партия лобстера = 300 мин, но приносит как 150 томатов). Ранние тиры не устаревают: их держит спрос, кооп-заказы и ностальгия-бонусы (E9). Валовые `$`/час — **до** вычета стоимости ингредиентов; чистая маржа высоких тиров ниже из-за длинных цепочек крафта (`05-ingredients`), что дополнительно сглаживает прогрессию.

### 3.3 Кривая стоимости апгрейдов (×2.2/уровень, канон §2.2)

Главный **Bucks-синк** и антиинфляционный якорь. Стоимость перехода на следующий уровень:
```
upgrade_cost(L → L+1) = base_cost × 2.2^(L-1)
```
где `base_cost` — цена первого апгрейда конкретного объекта (земля, станок, слот, склад). Множитель `×2.2` — каноничный (§2.2), един для всех апгрейдируемых объектов; отличается только `base_cost` (см. `04-machines` для станков, `02-farm` для земли/грядок).

Пример развёртки для `base_cost = $100` (эталонная кривая, см. §4.4 полную таблицу):

| L→L+1 | Стоимость `$` | Кумулятивно `$` |
|---|---|---|
| 1→2 | 100 | 100 |
| 2→3 | 220 | 320 |
| 3→4 | 484 | 804 |
| 5→6 | 2,342 | 5,489 |
| 8→9 | 24,940 | 55,441 |
| 9→10 | 54,868 | 110,308 |

> Экспонента синка растёт быстрее, чем линейно-накапливаемый доход игрока → «денежная яма» всегда на 2–3 недели впереди дохода. Это и есть встроенная защита от инфляции: заработал больше — следующий шаг дороже ровно настолько, чтобы темп прогресса оставался пологим.

### 3.4 XP и уровень фермы

`Farm Level (Уровень фермы)` — гейт контента (открывает грядки, постройки, тиры, штаты). **Отдельно** от `Farm Value` (§3.5) и от денег. XP не тратится, только копится.

**Источники XP (гипотеза):**

| Действие | XP | Примечание |
|---|---|---|
| Собрать грядку / продукт | `+1 × tier` | T1=+1 … T5=+5 |
| Приготовить блюдо | `+2 × tier` | поощряет крафт, не сбор |
| Продать блюдо (прилавок/смена) | `+1 × tier` | |
| Закрыть Co-op Order (`ui_coop_orders`) | `+25` | соц-действие ценнее |
| Заявка в конкурс (`09-fair`) | `+40` | |
| Возврат экспедиции (`07-expeditions`) | `+15 × state_tier` | |
| Daily Special выполнен (`mech_daily_special`) | `+20` | |
| Веха Appetite Meter (личная) | `+50` | серверный ивент |

**Кривая уровня (гипотеза):**
```
xp_to_next(L) = round(80 × 1.18^(L-1))     // рост ≈ ×1.18/уровень
```
Кэп MVP — **уровень 40** (гипотеза). Табличная развёртка — §4.5.

### 3.5 Farm Value (`mech_farm_value`) — сводная «мощь»

Не валюта. Скалярная сумма ценности активов, используется для **матчмейкинга лиг** и статуса (канон §3, `mech_farm_value`). Считается server-side.
```
FarmValue = Σ(building_value) + Σ(machine_value × level) + Σ(land_value)
          + Σ(staff_value × level) + Σ(know_how_value) + 0.5 × Σ(cosmetic_value)
```
| Компонент | Вес в FarmValue (гипотеза) |
|---|---|
| Постройка (`bld_*`) | = её кумулятивная стоимость `$` |
| Станок / земля | = кумулятивная стоимость апгрейдов |
| Стафф | = 200 × уровень (гипотеза) |
| Ветка Know-How узел | = 150 за узел (гипотеза) |
| Косметика | = 0.5 × её Dimes-цена, пересчитанной в `$` по курсу-ориентиру (§3.8) |

> **Гардрейл (канон §4, D11):** лиги считаются по **историческому вкладу** (в ивенты/котёл), FarmValue — лишь для *грубого матчмейкинга*, чтобы киты не давили новичков в одной лиге. Косметика входит с весом 0.5, чтобы «плативший за красоту» не считался сильнее «игравшего».

### 3.6 Рынок спроса (Demand Board) — генерация недельного тренда

**Категории (канон-совместимо с `09-fair` §4.2) — 4 меты:**

| Категория | EN / RU | Что входит |
|---|---|---|
| `cat_grill` | Grill / Гриль | бургеры, стейки, бекон, брискет |
| `cat_bakery` | Bakery & Desserts / Выпечка и десерты | пироги, пирожные, мороженое, сандеи |
| `cat_drinks` | Drinks / Напитки | содовая, малты, шейки, кофе |
| `cat_produce` | Produce & Sides / Овощи и гарниры | салаты, картофель, кукуруза, гарниры |

**Алгоритм генерации `D_cat` (детерминированный, server-side, Пн 00:00 UTC ролловер):**

```
seed = hash(week_number, town_id)          // одинаков для всего города, разный по городам/неделям
rng  = seededRNG(seed)

// 1. Сырые тяготения по 4 категориям
for cat in [grill, bakery, drinks, produce]:
    raw[cat] = rng.uniform(-1.0, +1.0)

// 2. Зеро-сумная нормировка (защита от глобальной инфляции спроса)
mean = average(raw)
for cat: centered[cat] = raw[cat] - mean          // сумма отклонений = 0

// 3. Масштаб в диапазон ±15…30% и клип
for cat:
    spread   = rng.uniform(0.15, 0.30)            // амплитуда недели (канон ±15–30%)
    D_cat    = clamp(1.0 + centered[cat] × spread × 1.7, 0.70, 1.30)
```

**Свойства (контракт):**
- Детерминизм от `(week, town)` → все игроки города видят **одну доску**; стрит может делить категории заранее. Разные города — разный спрос (основа для `State Fair Showdown`, `ev_state_fair_showdown`).
- **Зеро-сумность:** отклонения центрированы к 0 → в среднем спрос по городу нейтрален. Одна категория взлетела — другая просела. Нет недели, где «всё дорого» (это была бы инфляция).
- Диапазон жёстко зажат `[0.70, 1.30]` (канон §2.3: ±15–30% как максимум амплитуды на категорию).
- Тема недели для конкурса `Pie of the Week` (`ct_pie_week`) берётся тем же сидом (согласовано с `09-fair` §3.8): `theme = themePool[ hash(week, town) mod len(themePool) ]`.

### 3.7 Ностальгия-бонусы T1–T2 (анти-устаревание ранних тиров, E9)

Поверх категорийного `D_cat` каждую неделю **1–2 конкретных продукта** тира T1–T2 получают точечный **×2 спрос** (дек: «домашний лимонад T1 ×2»).

```
nostalgia_pool = все продукты с tier ∈ {T1, T2}
count = 1 + (rng.uniform(0,1) < 0.5 ? 1 : 0)        // 1 или 2 продукта/неделю
picks = rng.sample(nostalgia_pool, count)
for item in picks:
    D_item = 2.0        // мультипликативно поверх D_cat его категории (итог до ~2.6)
```
| Параметр | Значение (гипотеза) |
|---|---|
| Множитель спроса | ×2.0 к `p_ref` продукта в формуле продаж |
| Кол-во продуктов/неделю | 1–2 |
| Пул | только T1–T2 |
| Анонс | Demand Board + радио WSUN «на этой неделе все хотят…» |
| Стэкинг | мультипликативно с `D_cat`; итоговый эффективный спрос капится на **2.6** (пол/потолок продаж всё равно ограничен `S_sat`) |

> Экономический смысл: огород новичка (T1) регулярно становится **дефицитным товаром города**, а не только его личной едой. Это делает катч-ап органическим — старшие игроки *покупают* у новичка через кооп-заказы/прилавок.

### 3.8 Экономика Dimes — прайсинг ускорений (формула цена-от-времени)

Даймы покупают **оставшееся время** любого таймера (рост культуры, готовка, экспедиция, стройка). Цена считается от **остатка**, не от полной длительности — по мере тика таймера цена падает (честный UX, Hay-Day-модель).

```
dimes_to_finish(t_remaining_min) =
    if t_remaining_min ≤ 1:  0                        // последняя минута — бесплатно
    else:                    ceil(0.41 × t_remaining_min^0.53)
```

Калибровочные точки (вогнутая кривая: длинный таймер дороже, но per-минута дешевеет):

| Остаток | Стоимость `◉` | `◉`/час эффективно |
|---|---|---|
| 5 мин | 1 | 12.0 |
| 15 мин | 2 | 8.0 |
| 60 мин (1 ч) | 4 | 4.0 |
| 240 мин (4 ч) | 8 | 2.0 |
| 480 мин (8 ч) | 12 | 1.5 |
| 1440 мин (24 ч) | 20 | 0.83 |
| 4320 мин (3 дня) | 37 | 0.51 |

**Свойства прайсинга:**
- Показатель `b = 0.53 < 1` → **вогнутость**: скипнуть 24 ч не в 288 раз дороже, чем 5 мин, а лишь в ~20 раз. Дорогие длинные таймеры «прощаются» дешевле за минуту → игрок скорее скипнет длинную ночную партию, чем 5-минутную (которую проще подождать).
- Цена **пересчитывается в реальном времени** от остатка → нет ощущения «переплатил за уже прошедшее».
- **Батч-скип** (несколько слотов разом): сумма поштучных цен, **без** скидки (анти-абуз массового скипа перед ярмаркой; дневной кэп — §3.10).

**Прочие Dimes-синки (не время):**

| Синк | Цена `◉` (гипотеза) | Тип |
|---|---|---|
| +1 грядка (Garden Plot) сверх бесплатных | 25 → 40 → 60 → … (×1.5/слот) | ёмкость |
| +1 слот очереди станка | 30 → 45 → 68 → … (×1.5) | ёмкость |
| +1 маршрут экспедиции | 80 | ёмкость |
| Мгновенное расширение склада (Silo/Icehouse) на 1 шаг | = dimes_to_finish(время апгрейда) | время |
| Prize Machine — 1 пулл (косметика) | 12 | косметика/гача |
| Косметик-сет (Googie/Chrome/Tiki/Xmas-55) | 150–400 за сет | самовыражение |
| Route Pass — платный трек (сезон) | 900 `◉` или прямой IAP | сезон |

> **Курс-ориентир для баланса (не игровой обмен!):** `1 ◉ ≈ $50 Bucks-эквивалента` по «стоимости сэкономленного времени» на T3-производстве (гипотеза). Используется только для расчёта FarmValue-веса косметики (§3.5) и для калибровки, **в игре обмена нет**.

### 3.9 Экономика Tickets и Ribbons

**Tickets (`🎟`) — ивент-контур:**

| Источник | `🎟` (гипотеза) |
|---|---|
| Личная веха вклада в Appetite Meter | 5 / 10 / 15 / 20 по вехам 25/50/75/100% |
| Завершение недельного ритуала (ярмарка+ивент) | 8 |
| Порог очков смены (500 → +1, кэп 5/неделя, `09-fair`) | до 5 |
| Конверсия вклада в town projects при переезде (D12, «ничего не сгорает») | = 0.1 × вложенных `$` в проекты |
| Route Pass фри-трек (тики уровней) | 2–4 за уровень |

| Синк | `🎟` (гипотеза) |
|---|---|
| Ивент-лавка: редкий декор/сезонная косметика | 20–120 |
| Ивент-лавка: пакет семян T3–T4 | 15 |
| Страховка/заморозка стрика (`mech_regular_streak`, E2) | 5 за заморозку |

**Ribbons (`🎀`) — чистый престиж:** источник — **только** победа/призовое место в конкурсах ярмарки (`ct_pie_week`, `ct_giant_veg`, `ct_best_window`). Синк — **нет**. Отображаются на `ui_ribbon_wall`. Не влияют ни на что, кроме статуса и рамок профиля (канон §2.1, гардрейл: судят качество, не количество).

### 3.10 Катч-ап новичка: Grand Opening ×2 (`mech_grand_opening`)

| Параметр | Значение (гипотеза) |
|---|---|
| Триггер | старт игры (после Tutorial Mini-Week, `01-core-loop` §3.9); Town Merge; Street Caravan (D12) |
| Эффект | **×2 ко всему Bucks-доходу** (продажи прилавка + чаевые смены + продажи кооп-заказов) |
| Длительность | **7 дней** (одна серверная неделя) |
| НЕ влияет на | Dimes, Tickets, Ribbons, XP; не пробивает кэпы; не удваивает спрос |
| Стэкинг | не стэкается сам с собой; при повторном триггере (мердж во время активного) — продлевает до 7 дней от новой даты |
| Тон (канон E8) | «Гранд-опенинг твоего дайнера!» — праздник, не костыль |

Плюс постоянный катч-ап через **ностальгия-бонусы** (§3.7): спрос на T1–T2 регулярно поднят → ранний ассортимент новичка ликвиден.

### 3.11 Анти-инфляция и потолки (сводные механики)

Дизайн держит Bucks-инфляцию через **семь встроенных дренажей и ограничителей**:

1. **Экспоненциальный синк апгрейдов ×2.2/ур (§3.3)** — главный дренаж; растёт быстрее дохода.
2. **Зеро-сумный рынок спроса (§3.6)** — сумма отклонений = 0, нет «недели всеобщего богатства».
3. **Перенасыщение `S_sat` (§4.6)** — избыточная категория дешевеет (пол 0.40), самокоррекция цен.
4. **Ценовой ползунок зажат `0.70×…1.50×` (`09-fair`)** — игрок не может выставить произвольно высокую цену.
5. **Mastery повышает эффективность, не тираж (`mech_mastery`, канон)** — гринд не раздувает объём денег в системе.
6. **Дневные кэпы на бусты (§4.7)** — ограничивают, сколько времени/дохода можно «выкупить» за день.
7. **Замкнутость `🎟`/`🎀` и запрет `Dimes→Bucks`** — премиум и престиж-контуры не вливают Bucks в экономику.

**Целевой баланс faucet/drain (гипотеза):** синки должны поглощать **≈85–95%** заработанных Bucks у активного игрока в фазе роста, оставляя чистый прирост net-worth **≈+8–12%/неделю** (пологая прогрессия, «мечта всегда чуть впереди»). Точная калибровка — на плейтесте.

**Явные потолки (гипотеза):**

| Потолок | Значение |
|---|---|
| Макс. `D_cat` на категорию | 1.30 |
| Мин. `D_cat` | 0.70 |
| Макс. точечный ностальгия-спрос (эффективный) | 2.6 |
| Пол/потолок `S_sat` | 0.40 / 1.15 |
| Ценовой ползунок прилавка | 0.70× … 1.50× `p_ref` |
| Grand Opening | ×2, только Bucks, 7 дней |
| Тикеты со смены | 5/неделя |
| Уровень фермы (MVP) | 40 |

---

## 4. Данные и формулы (таблицы)

### 4.1 Опорная таблица цен и производных (финал канон §2.2)

| Метрика | T1 | T2 | T3 | T4 | T5 |
|---|---|---|---|---|---|
| `p_ref` блюдо, `$` | 6 | 22 | 75 | 260 | 900 |
| ×к T1 (ценность/клик) | ×1 | ×3.7 | ×12.5 | ×43 | ×150 |
| Цикл блюда, мин | 5 | 15 | 45 | 120 | 300 |
| Валовый `$`/час (1 слот) | 72 | 88 | 100 | 130 | 180 |
| ×к T1 (доход/час) | ×1.00 | ×1.22 | ×1.39 | ×1.81 | ×2.50 |
| Референс-стоимость ингредиентов, `$` (гипотеза) | 1 | 6 | 24 | 95 | 360 |
| Чистая маржа/блюдо, `$` (гипотеза) | 5 | 16 | 51 | 165 | 540 |
| Чистый `$`/час (1 слот) | 60 | 64 | 68 | 82.5 | 108 |

> Чистый `$`/час T1→T5 = ×1.8 (ещё положе валовых ×2.5) — цепочки ингредиентов высоких тиров дополнительно съедают преимущество, усиливая «горизонтальный выбор тира» вместо «верхний тир всегда лучше».

### 4.2 Кривая апгрейдов ×2.2 (эталон base=$100), уровни 1–10

| L→L+1 | Стоимость `$` | Кумулятивно `$` |
|---|---|---|
| 1→2 | 100 | 100 |
| 2→3 | 220 | 320 |
| 3→4 | 484 | 804 |
| 4→5 | 1,065 | 1,869 |
| 5→6 | 2,342 | 4,211 |
| 6→7 | 5,153 | 9,364 |
| 7→8 | 11,336 | 20,700 |
| 8→9 | 24,940 | 45,640 |
| 9→10 | 54,868 | 100,508 |

Абсолютные `base_cost` по объектам (сведение из профильных спек; здесь — источник истины):

| Объект | `base_cost` `$` (гипотеза) | Источник прогрессии |
|---|---|---|
| Grill (Гриль) | 60 | `04-machines` §дал ×2.2 |
| Oven (Печь) | 90 | `04-machines` |
| Churn (Маслобойка) | 70 | `04-machines` |
| Soda Fountain (Автомат газировки) | 35 | `04-machines` |
| Ice Cream Maker (Мороженица) | 75 | `04-machines` |
| Coffee Percolator (Кофеварка) | 150 | `04-machines` |
| Поздние станки T4–T5 | 300–600 | `04-machines` |
| Garden Plot (Грядка) — расширение | 40 | `02-farm` |
| Barn/Coop слот животного | 120 | `03-animals` |
| Silo/Icehouse шаг ёмкости | 80 | `02-farm` |
| Fair Stall (Палатка ярмарки) | 100, шаг ×2.7…3 | `09-fair` §3.6 |

### 4.3 XP-кривая (уровни фермы, вехи)

`xp_to_next(L) = round(80 × 1.18^(L-1))`

| Уровень | XP до следующего | Кумулятивно XP | Типовой контент-анлок (гипотеза) |
|---|---|---|---|
| 1→2 | 80 | 80 | 2-я грядка |
| 3→4 | 111 | 300 | Kitchen, первый станок T2 |
| 5→6 | 155 | 570 | Diner (прилавок) |
| 8→9 | 253 | 1,290 | Garage → экспедиции T3 |
| 10→11 | 352 | 1,950 | Barn, животные |
| 15→16 | 805 | 5,300 | штаты T4 (Georgia) |
| 20→21 | 1,842 | 12,500 | Icehouse, T4-станки |
| 30→31 | 9,650 | 62,000 | штаты T5 (Maine) |
| 39→40 | 42,600 | 285,000 | кэп MVP, T5-легенды |

### 4.4 Симуляция типовой недели по фазам игрока

Центральная эконом-таблица баланса. Значения — **гипотезы для первой итерации**, калибруются плейтестом. «Активных слотов» = грядки + станки + животные, генерящие доход параллельно.

| Фаза | День игры | Осн. тир | Активных слотов | Сессий/день | Buck-доход/день `$` | **Bucks/неделю** `$` | Осн. синки недели | Dimes/нед (фри) `◉` |
|---|---|---|---|---|---|---|---|---|
| **День 1** (новичок, Grand Opening ×2) | 1–7 | T1 (+чуть T2) | 4–6 | 2 | ~180 (уже ×2) | **~1,300** | 2-я грядка, 1-й станок T2 | 3–5 |
| **Неделя 2** (обжился) | 8–21 | T1–T2 | 8–12 | 2–3 | ~800 | **~5,500** | Diner, Kitchen-апгрейды, silo | 4–6 |
| **Месяц 2** (мид) | 30–60 | T2–T3 | 16–24 | 3 | ~4,800 | **~34,000** | Garage, T3-станки, стафф, экспедиции | 6–10 |
| **Эндгейм** (оптимизатор) | 90+ | T4–T5 | 30–45 | 3–4 | ~28,000 | **~200,000** | T5-станки, Icehouse, косметика, town projects | 10–15 |

**Производные метрики баланса (гипотеза):**

| Метрика | День 1 | Неделя 2 | Месяц 2 | Эндгейм |
|---|---|---|---|---|
| Валовый `$`/час активной игры | ~90 | ~350 | ~1,900 | ~9,000 |
| Кол-во кооп-заказов/нед | 0–1 | 2–3 | 5–8 | 8–12 |
| Доля дохода с прилавка (пассив) | 60% | 55% | 50% | 45% |
| Доля дохода со смены (актив) | 25% | 25% | 25% | 25% |
| Доля дохода с кооп/ивент | 15% | 20% | 25% | 30% |
| Целевой net-worth прирост/нед | +40% (буст) | +18% | +12% | +9% |

> Прогрессия дохода День1→Эндгейм ≈ **×150** по недельным Bucks — совпадает с масштабом «×150 ценность/клик»: игрок не столько «зарабатывает в час больше», сколько **оперирует более дорогими единицами** на большем числе слотов. Доля пассива падает (60%→45%), доля кооп/ивента растёт (15%→30%) — экономика сдвигается от соло-грядки к городской кооперации (пилларс P3).

### 4.5 Формула продаж прилавка (ссылка-контракт с `09-fair`)

Экономика цен реализуется в механике продаж ярмарки (`09-fair` §3.3). Здесь — эконом-контракт входных множителей (числа отсюда — источник истины):

```
SellRate(units/h) = R_base(tier) × D_cat × D_item × P_price × Q_quality × S_sat × L_stall × Staff
```

| Множитель | Диапазон | Источник (истина) |
|---|---|---|
| `R_base(tier)` | 8.0 / 3.0 / 1.2 / 0.40 / 0.15 (T1..T5) | эта спека + `09-fair` §4.1 |
| `D_cat` | 0.70 … 1.30 | §3.6 (генерация здесь) |
| `D_item` | ×2.0 (ностальгия) или 1.0 | §3.7 (здесь) |
| `P_price` | `(p_ref/p_set)^1.8`, ε=1.8 | `09-fair` §4.3 |
| `Q_quality` | `1 + 0.08×★` (1.00…1.40) | `09-fair` §4.4 |
| `S_sat` | `clamp((Demand/Listed)^0.5, 0.40, 1.15)` | §4.6 (здесь) |
| `L_stall` | 1.00 … 1.30 (уровень палатки) | `09-fair` §3.6 |
| `Staff` | множитель постов Counter | `03-animals`/стафф-спека |

### 4.6 Эластичность перенасыщения `S_sat` (анти-флуд, E7)

```
S_sat(cat) = clamp( (Demand_units_cat / Listed_units_cat)^0.5 , 0.40 , 1.15 )
```
где `Demand_units_cat` — целевой городской спрос категории на неделю (функция от `D_cat` и числа активных игроков), `Listed_units_cat` — суммарно выставленный городом сток категории.

| Listed / Demand | `S_sat` | Эффект на скорость продаж |
|---|---|---|
| 0.5× (дефицит) | 1.15 (потолок) | бонус: закрыл дефицитную мету |
| 1.0× (баланс) | 1.00 | нейтрально |
| 2.0× (перекорм ×2) | 0.71 | −29% скорость |
| 4.0× (флуд ×4) | 0.50 | −50% |
| ≥6.25× | 0.40 (пол) | самое дно, но продажи не 0 |

> Считается **на весь город по 4 метам**, не по игроку. Доминировать одной метой невыгодно (мягко дешевеет), но никогда не смертельно (пол 0.40) — позитив-сумность (P3): даже «проиграв» гонку категории, игрок что-то продаёт.

### 4.7 Дневные кэпы на бусты (анти-инфляция §3.11.6)

| Бустер | Дневной кэп (гипотеза) | Валюта | Общий кэп? |
|---|---|---|---|
| Overtime (овертайм станка, `04-machines`) | 3 применения/день | `◉` или `$` | **общий пул бустов = 6/день** |
| Fertilizer (удобрение, `02-farm`) | 4 применения/день | `$`/`◉` | входит в общий пул |
| Rush-контракт экспедиции (`07-expeditions`) | 2/день | `◉` | входит в общий пул |
| Instant-skip таймеров (§3.8) | без штучного кэпа, но батч-скип без скидки | `◉` | вне пула (платный) |

> «Общий пул бустов = 6 применений/день» (гипотеза) — чтобы кит не «выкупал» неограниченный дневной прогресс. Instant-skip таймеров вне пула (это чистая продажа времени), но батчем без скидки — трение против массового скипа перед ярмаркой.

### 4.8 Сводка источников и синков по валютам

**Bucks `$` (источники):**

| Источник | Порядок величины |
|---|---|
| Прилавок ярмарки (пассив, `09-fair`) | 45–60% дохода |
| Смена у прилавка (актив, чаевые) | ~25% |
| Co-op Orders (`ui_coop_orders`) | 10–20% |
| Daily Specials (`mech_daily_special`) | разовые бонусы |
| Продажа излишков соседям/каталог (`08-mail-foraging`) | мелкий |

**Bucks `$` (синки):**

| Синк | Порядок |
|---|---|
| Апгрейды земли/станков/слотов ×2.2/ур | **главный (≈70% трат)** |
| Постройки (`bld_*`) | крупные вехи |
| Стафф (жетоны + `$`-апгрейд) | средний |
| Know-How (research) | средний |
| Ингредиенты/семена/корм | оборотный |
| Взнос в town projects | соц-синк (возвращается `🎟` при переезде) |

**Dimes `◉` (источники):** покупка за реал (основной); фри-трикл — дейли-логин (1–2/день гипотеза), Route Pass фри-трек, вехи ивента, дейли-пулл Prize Machine (бесплатная попытка). **Синки:** §3.8.

**Tickets `🎟` / Ribbons `🎀`:** §3.9.

### 4.9 Route Pass и Prize Machine (эконом-параметры)

| Параметр | Значение (гипотеза) |
|---|---|
| Длина сезона Route Pass | 8–10 недель (канон, `01-core-loop` §3.8) |
| Цена платного трека | 900 `◉` или прямой IAP-эквивалент |
| Прогресс трека | от недельных ритуалов (ярмарка/ивент), **не** от гринда/спенда |
| Prize Machine — цена пулла | 12 `◉` (косметика only) |
| Prize Machine — дейли-фри | 1 пулл/день |
| **Открытый pity** | гарант. rare каждые **30 пуллов** (счётчик виден в UI, канон-гардрейл; закрывает Открытый вопрос §8.6 канона — цифра-гипотеза) |
| Дубли | → scrap → обмен на выбранную фигурку |

---

## 5. UI-точки

| Экран (ключ) | Что показывает из экономики |
|---|---|
| `ui_demand_board` (Demand Board / Доска спроса) | 4 меты с `D_cat` (стрелки ±%), ностальгия-продукты недели (×2), тема конкурса; анонс DJ Ricky Ray |
| `ui_fair_stall` (Fair Stall / Прилавок ярмарки) | ползунок цены 0.70×–1.50× с прогнозом «продастся ≈N/окно» (пересчёт `SellRate` в реалтайме под спрос+насыщение) |
| HUD валют (глобальный) | `$` `◉` `🎟` `🎀` моноширинно (`tabular-nums`, канон §2.1); тап → история операций |
| Любой таймер (грядка/станок/экспедиция/стройка) | кнопка «Finish ◉N» с ценой от остатка (§3.8), пересчёт в реалтайме; последняя минута — «Finish (free)» |
| Экран апгрейда (станок/земля/склад) | текущая цена `$` (×2.2), предпросмотр следующего уровня, «до апгрейда осталось накопить $X» |
| Профиль / `mech_farm_value` | Farm Value как статус (не рейтинг силы), лига |
| `ui_route_pass` | фри/платный трек, прогресс от ритуалов |
| `ui_prize_machine` | **видимый счётчик pity** (X/30 до rare), дейли-фри-таймер |
| `ui_ribbon_wall` | ленты с годом/номинацией (престиж, не тратятся) |
| Weekly Check (сводка недели, `09-fair` §4.10) | разбивка дохода: прилавок / смена / кооп / ивент; net-worth дельта недели |

---

## 6. Зависимости от других систем

| Файл спеки | Что берёт из экономики / что отдаёт сюда |
|---|---|
| `docs/specs/00-canon.md` | §2.1 валюты, §2.2 опорные цены/множители/циклы, §4 гардрейлы монетизации — **истина**, эта спека их разворачивает |
| `docs/specs/01-core-loop.md` | календарь недели (ролловер Пн 00:00 UTC = генерация Demand Board); Grand Opening-триггер после Tutorial Mini-Week; сезон Route Pass |
| `docs/specs/02-farm.md` | `base_cost` грядок/склада; тайминги роста (вход в Dimes-скип); Fertilizer-кэп |
| `docs/specs/03-animals.md` | доход/час животных (сюда — финал калибровки, §4.4); стафф-эффекты |
| `docs/specs/04-machines.md` | `base_cost` станков и прогрессия ×2.2 (числа финализируются здесь); Overtime-кэп |
| `docs/specs/05-ingredients.md` | референс-стоимости ингредиентов (§4.1 чистая маржа); длина цепочек крафта |
| `docs/specs/07-expeditions.md` | доход/цены регионалки T3–T5; Rush-контракт (Dimes-буст, кэп); слот маршрута (Dimes-синк) |
| `docs/specs/08-mail-foraging.md` | каталог/фуражинг как мелкий источник; цены редких семян |
| `docs/specs/09-fair.md` | **двусторонний контракт:** формула `SellRate`, `R_base`, `P_price`, `Q_quality`, палатка; сюда — `D_cat`, `D_item`, `S_sat`, `p_ref` |
| Demand Board spec (гипотеза номера) | владелец UI доски; `D_cat`/тема — генерация здесь (§3.6) |
| Co-op Orders spec (гипотеза номера) | цены/награды кооп-заказов; доля дохода (§4.4) |
| Event spec (`10-event`, гипотеза) | Appetite Meter вехи → `🎟`; лиги по вкладу (не спенду) |
| Staff spec (гипотеза номера) | стафф-множители продаж/производства; жетоны + `$`-апгрейд |
| Route Pass / Prize Machine / монетизация spec (гипотеза номера) | цены `◉`, pity=30, косметик-сеты (§4.9) |

---

## 7. Edge cases

| # | Ситуация | Поведение экономики |
|---|---|---|
| EC1 | Спрос-сид дал экстремум (все 4 меты у границ) | Зеро-сумная нормировка (§3.6 шаг 2) гарантирует сумму отклонений = 0; клип `[0.70,1.30]` не даёт вылет. «Недели всеобщего богатства» невозможны by design. |
| EC2 | Весь город флудит одну мету (мета-война) | `S_sat → 0.40` (пол): категория дешевеет на −60% скорости, но продажи не 0. Дефицитные меты дают +15% — рынок самобалансируется (E7). |
| EC3 | Игрок пытается «отмыть» Dimes в Bucks через скип+продажу | Скип экономит время, но Bucks всё равно из продаж по рыночной цене с кэпами/насыщением. Прямого `Dimes→Bucks` нет; арбитраж ограничен `S_sat` и кэпом бустов (§4.7). |
| EC4 | Новичок в конце недели (Grand Opening обрезан ролловером?) | Grand Opening — **7 дней от активации** (не привязан к серверной неделе), переживает ролловер (§3.10). |
| EC5 | Ностальгия-продукт совпал с уже-высоким `D_cat` его категории | Стэк мультипликативен, но эффективный спрос капится на 2.6 (§3.7) + `S_sat` всё равно душит перекорм → без рант-эффекта. |
| EC6 | Инфляция: старый игрок с горой Bucks | Синк ×2.2/ур (§3.3) делает следующий апгрейд дороже накопленного; Bucks некуда «слить в силу» (нет `Bucks→Dimes/Ribbons`). Богатство конвертируется в косметику/town projects (соц-синк). |
| EC7 | Переезд (Moving Truck / Caravan): что с вложенным в town projects `$`? | Не сгорает: конвертируется в `🎟` по `0.1×вложенных $` (D12, §3.9) + Grand Opening ×2 на новом месте. |
| EC8 | Пропущенная неделя (игрок офлайн) | Прилавок продавал сток пассивно по спросу той недели (офлайн-доход, `09-fair`); стрик замерзает (E2), не обнуляется. Дохода меньше, наказания нет. |
| EC9 | Дефляция цены при массовом скипе перед ярмаркой | Батч-скип без скидки (§3.8) + `S_sat` на резко выросший Listed → цена категории падает. Массовый Dimes-скип самонаказуем рынком. |
| EC10 | Два города — разный спрос (State Fair Showdown) | Сид включает `town_id` → доски различаются; это фича для versus-ивента (`ev_state_fair_showdown`), не баг. |
| EC11 | Кит скупает все Prize-пуллы | Косметика only, pity открыт (30), нулевое влияние на FarmValue-силу (косметика вес 0.5) и лиги (по вкладу). Статус Fair Patron, не превосходство (E10 канон). |
| EC12 | Отрицательный баланс / покупка в минус | Невозможна: транзакция отклоняется, если недостаёт валюты. Банкротства нет (позитив-сум). |

---

## 8. Открытые вопросы

1. **Номер файла — блокер консистентности.** Все сиблинг-спеки (`03/04/09` и др.) ссылаются на `docs/specs/12-economy.md`; этот файл создан как `14-economy.md` (по заданию). Нужно решение: переименовать в `12-economy.md` **или** массово обновить ссылки на `14-economy.md`. До разрешения — считать одним документом. (Расхождение с правилом канона §6.2.)
2. **Курс `1 ◉ ≈ $50` (§3.8).** Использован только для веса косметики в FarmValue и калибровки. Подтвердить величину плейтестом; убедиться, что он нигде не превращается в фактический игровой обмен (иначе — дыра в анти-P2W D11).
3. **Прайсинг Dimes `dimes = ceil(0.41 × t^0.53)` (§3.8).** Константы `a=0.41, b=0.53` — гипотеза под калибровочные точки. Проверить монетизацию: не слишком ли дёшев скип ночных T5-таймеров (37 `◉` за 3 дня)?
4. **Общий пул бустов 6/день (§4.7).** Согласовать точное число и состав пула с `02-farm`/`04-machines`/`07-expeditions` — не конфликтует ли с их локальными кэпами.
5. **Целевой net-worth прирост +8–12%/нед (§3.11).** Ключевой параметр темпа прогрессии; валидировать симуляцией недельной петли по всем 4 фазам (§4.4) до плейтеста.
6. **Pity Prize Machine = 30 (§4.9).** Введено как цифра-гипотеза, закрывающая Открытый вопрос §8.6 канона. Требует PR в канон §3.10/§4 для фиксации официального числа pity.
7. **Ностальгия-бонус ×2, 1–2 продукта/нед (§3.7).** Достаточна ли амплитуда/частота, чтобы реально держать T1–T2 ликвидными на эндгейме (E9)? Возможна привязка к дефициту кооп-заказов вместо чистого рандома.
8. **Зеро-сумная нормировка спроса (§3.6).** Строгая зеро-сумность может ощущаться «плоско». Рассмотреть лёгкий положительный дрейф (+2–3% средний) в ивент-недели как контролируемый faucet — согласовать с анти-инфляцией §3.11.
9. **Доля дохода прилавок/смена/кооп (§4.4).** Целевые проценты (45–60/25/15–30) — гипотезы; финализировать после спек Co-op Orders и активной смены.
10. **Чистые маржи по тирам (§4.1).** Зависят от `05-ingredients` (стоимость цепочек). Сверить референс-стоимости ингредиентов, чтобы чистый `$`/час T1→T5 действительно вышел ×1.8, а не инвертировался на длинных цепочках T5.
